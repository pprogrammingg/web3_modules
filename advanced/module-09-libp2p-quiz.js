(function() {
    var questions = [
        { question: "In GossipSub, what is the 'mesh' for a topic?", options: ["The entire network", "The set of peers you are directly connected to for that topic; you exchange messages and control messages (graft/prune) with them", "The DHT routing table", "The bootstrap list"], correct: 1, explanation: "The mesh is the set of direct links for a given topic; it is per-topic and maintained by each peer." },
        { question: "What does a GRAFT message do?", options: ["Remove a peer from the mesh", "Add a peer to your mesh for a given topic so you will exchange messages for that topic with them", "Request content from the DHT", "Encrypt a channel"], correct: 1, explanation: "GRAFT adds a peer to the mesh for a topic." },
        { question: "What does a PRUNE message do?", options: ["Add a peer to the mesh", "Remove a peer from your mesh for that topic (the connection may still be used for other topics or protocols)", "Store a value in the DHT", "Negotiate security"], correct: 1, explanation: "PRUNE removes a peer from the mesh for that topic." },
        { question: "What is Prune Peer eXchange (PX) in GossipSub v1.1?", options: ["A new transport", "When you PRUNE a peer, you can include a list of alternative peers so they can connect elsewhere and rejoin the mesh; reduces dependence on external discovery", "A security handshake", "A DHT operation"], correct: 1, explanation: "PX sends peer information along with PRUNE to help the pruned peer find new mesh links." },
        { question: "Why is prune backoff used?", options: ["To speed up mesh formation", "To prevent rapid graft–prune cycles and limit abuse (e.g. a pruned peer cannot be grafted back immediately)", "To replace the DHT", "To encrypt messages"], correct: 1, explanation: "Backoff enforces a cooldown after prune so the same peer does not immediately re-graft." },
        { question: "What are IHAVE and IWANT used for in GossipSub?", options: ["Security handshakes", "Lazy propagation: IHAVE advertises message IDs you have; IWANT requests full messages, reducing bandwidth when many peers already have the message", "DHT lookups", "Transport selection"], correct: 1, explanation: "IHAVE/IWANT allow compact advertisement and on-demand delivery of messages." },
        { question: "How is the mesh kept sparse?", options: ["By using only one peer per topic", "By maintaining a bounded number of mesh peers per topic (min/max) and using graft/prune to add or remove peers", "By disabling gossip", "By using only the DHT"], correct: 1, explanation: "Mesh size parameters and graft/prune keep the mesh sparse but connected." },
        { question: "What kind of attack does peer scoring in GossipSub help mitigate?", options: ["Only eclipse", "Spam and abusive behavior: malicious peers can be downgraded or pruned based on scoring", "Only DHT poisoning", "Only MITM"], correct: 1, explanation: "Scoring (e.g. for spam, invalid messages) lets the network penalize and prune bad peers." },
        { question: "What is an eclipse attack in the context of P2P?", options: ["A solar eclipse affecting connectivity", "Attackers surrounding a victim with malicious peers so the victim only sees attacker-controlled traffic", "A type of DHT lookup", "A transport protocol"], correct: 1, explanation: "Eclipse is when an attacker controls all or most of a victim's peers." },
        { question: "How can diverse discovery (DHT, bootstrap, PX) help against eclipse?", options: ["It cannot", "By giving a peer multiple ways to find peers, it is harder for an attacker to control all of the victim's connections", "By encrypting all traffic", "By using only one discovery method"], correct: 1, explanation: "Multiple discovery sources make it harder to fully eclipse a node." },
        { question: "What does the relay protocol in libp2p provide?", options: ["A replacement for GossipSub", "A way for peers behind symmetric NAT to be reachable (traffic is relayed through a third peer)", "A new DHT", "Only TLS"], correct: 1, explanation: "Relay allows peers that cannot be dialed directly to receive connections via a relay node." },
        { question: "What is AutoNAT in libp2p?", options: ["A transport", "A mechanism for a peer to discover whether it is reachable from the public internet (e.g. by asking other peers to dial it)", "A pub/sub topic", "A replacement for PeerId"], correct: 1, explanation: "AutoNAT helps a node determine if it is behind NAT and needs relay or other help." },
        { question: "When designing a protocol on libp2p, why use a versioned protocol ID (e.g. /myapp/1.0.0)?", options: ["Only for display", "So multistream-select can negotiate and you can evolve the protocol without breaking older peers", "To replace the DHT", "Only for security"], correct: 1, explanation: "Versioned protocol IDs allow backward-compatible upgrades and clear negotiation." },
        { question: "In a closed or permissioned validator network, how is peer discovery often done?", options: ["Only via the public DHT", "Via bootstrap list and/or config (known validator PeerIds and multiaddrs) rather than open DHT", "Only via mDNS", "Discovery is not needed"], correct: 1, explanation: "Permissioned networks often use configured bootstrap and known peer lists." },
        { question: "How might a consensus protocol use GossipSub?", options: ["Only for DHT lookups", "For block and transaction propagation: validators subscribe to topics and publish blocks/transactions; attestations or votes may use the same or separate topics", "Only for peer discovery", "Only for relay"], correct: 1, explanation: "GossipSub is commonly used for block and tx propagation and sometimes for votes/attestations." },
        { question: "Why might PeerId be derived from a consensus key (e.g. BLS public key)?", options: ["To avoid encryption", "So that application identity (validator ID) and network identity (PeerId) are tied; other validators can compute your PeerId from the known validator set", "To replace the DHT", "Only for relay"], correct: 1, explanation: "Deriving PeerId from consensus key binds network identity to protocol identity (e.g. Hyperscale-rs)." },
        { question: "What metrics are useful when operating a libp2p-based network in production?", options: ["Only latency", "Connection counts, peer counts per topic, message rates, graft/prune rates, peer scoring state", "Only throughput", "Only disk usage"], correct: 1, explanation: "Observability of connections, mesh state, message flow, and scoring helps operate and tune the network." },
        { question: "In GossipSub, is the mesh per-topic or global?", options: ["Global: one mesh for all topics", "Per-topic: each topic has its own mesh; a peer can be in different meshes for different topics", "Only one topic is allowed", "Mesh is only for the DHT"], correct: 1, explanation: "Each topic has its own mesh; a node can have different mesh neighbors for different topics." },
        { question: "What is the main benefit of lazy propagation (IHAVE/IWANT) compared to always sending full messages?", options: ["Faster security handshake", "Reduced bandwidth when many peers already have the message; full message is sent only when requested", "Replaces graft/prune", "Replaces the DHT"], correct: 1, explanation: "IHAVE/IWANT reduces redundant full-message traffic when many nodes already have the data." },
        { question: "When would you use request–response over a stream instead of pub/sub?", options: ["For all communication", "When you need a direct reply to a request (e.g. RPC, fetch one block by hash) rather than broadcast", "Only for discovery", "Only for relay"], correct: 1, explanation: "Request–response (streams) is for directed queries; pub/sub is for broadcast." }
    ];
    initializeQuiz('quiz-libp2p-advanced', questions, 70);
})();
