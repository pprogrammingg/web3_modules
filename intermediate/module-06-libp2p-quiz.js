(function() {
    var questions = [
        { question: "Which transport in libp2p typically requires a separate security handshake (Noise or TLS)?", options: ["QUIC only", "TCP and WebSocket (they do not have built-in encryption; libp2p runs Noise or TLS on top)", "All transports have built-in encryption", "Only WebRTC"], correct: 1, explanation: "TCP and WebSocket are plain; libp2p adds Noise or TLS. QUIC can already include encryption." },
        { question: "What is QUIC in the context of libp2p?", options: ["A consensus algorithm", "A transport built on UDP with potential built-in encryption; often lower latency and better for lossy networks", "A replacement for the DHT", "A pub/sub protocol"], correct: 1, explanation: "QUIC is a transport layer (UDP-based) that can carry TLS and multiplexing; it is an alternative to TCP." },
        { question: "Why would you use WebSocket as a libp2p transport?", options: ["It is always faster than TCP", "For browsers and environments where raw TCP is blocked; it runs over HTTP", "WebSocket does not require security", "Only for content routing"], correct: 1, explanation: "WebSocket is useful in restricted or browser environments where TCP might be blocked." },
        { question: "What is PeerId derived from?", options: ["The peer's IP address", "A public key (e.g. Ed25519, secp256k1); the PeerId is typically a hash or encoding of that key", "A random number chosen at startup", "The multiaddr"], correct: 1, explanation: "PeerId is the stable identity from the peer's public key." },
        { question: "What does a multiaddr encode?", options: ["Only the PeerId", "How to reach a peer: protocol stack such as IP, port, and transport (e.g. /ip4/192.168.1.1/tcp/4001)", "Only the public key", "Only the DHT key"], correct: 1, explanation: "Multiaddr is self-describing: it includes IP (or DNS), transport (TCP, etc.), and port or path." },
        { question: "Which security protocol in libp2p uses the Noise Protocol Framework?", options: ["Only TLS 1.3", "Noise (e.g. XX handshake, ChaChaPoly); protocol ID /noise", "Neither; libp2p has no security", "Only for QUIC"], correct: 1, explanation: "Noise is one of the two main security handshakes in libp2p; it uses the Noise framework." },
        { question: "How does TLS 1.3 in libp2p relate to PeerId?", options: ["TLS does not use PeerId", "The peer's libp2p public key can be encoded in the TLS certificate so the connection is bound to that PeerId", "PeerId is only used after TLS", "TLS replaces PeerId"], correct: 1, explanation: "libp2p can bind the TLS certificate to the peer's public key so that the secure channel is tied to PeerId." },
        { question: "What is stream multiplexing used for?", options: ["Only for video streaming", "Reusing one transport connection for many logical streams (multiple protocols or requests) to reduce connection overhead", "Replacing the DHT", "Only in the browser"], correct: 1, explanation: "Multiplexing lets many streams share one connection that is established and secured once." },
        { question: "In what order does connection establishment typically occur in libp2p?", options: ["Security first, then transport", "Transport connection, then security handshake (Noise or TLS), then multiplexing; then application protocols run over streams", "DHT first, then transport", "Multiplexing before security"], correct: 1, explanation: "Standard order: establish transport, run security handshake, then multiplex; app protocols run on top." },
        { question: "What is multistream-select used for?", options: ["Selecting which DHT to use", "Negotiating which application protocol to run on a new stream (e.g. /myapp/1.0.0) so peers agree on version and protocol", "Choosing a transport", "Selecting a PeerId"], correct: 1, explanation: "Multistream-select is the protocol negotiation step when opening a stream." },
        { question: "What does the libp2p DHT use for organizing the network?", options: ["A central server", "A Kademlia-style structure with XOR distance; nodes maintain routing tables (e.g. k-buckets)", "Only bootstrap nodes", "Random graph"], correct: 1, explanation: "The libp2p DHT is Kademlia-based; XOR metric and k-buckets give O(log n) lookups." },
        { question: "What is FIND_NODE in the DHT used for?", options: ["Finding file content", "Finding the k closest peers to a given key (e.g. PeerId); used for peer routing", "Finding the bootstrap list", "Finding TLS certificates"], correct: 1, explanation: "FIND_NODE is a Kademlia operation to locate peers close to a key; used for peer discovery." },
        { question: "What is content routing in the libp2p DHT?", options: ["Routing only peer IDs", "Finding which peers have or provide specific content (e.g. via ADD_PROVIDER, GET_PROVIDERS, and content IDs like CIDs)", "Routing TCP packets", "Only for encryption"], correct: 1, explanation: "Content routing answers 'who has this content?'; providers advertise and clients query." },
        { question: "What are PUT_VALUE and GET_VALUE in the DHT?", options: ["Transport protocols", "Operations to store and retrieve arbitrary signed values (e.g. peer records) in the DHT", "Security handshakes", "Pub/sub topics"], correct: 1, explanation: "PUT_VALUE/GET_VALUE allow storing and retrieving signed data in the DHT." },
        { question: "Besides the DHT, how can peers discover each other?", options: ["Only via the DHT", "Bootstrap list, mDNS (local), rendezvous (e.g. a designated node where peers register for a topic)", "Only via multistream-select", "Only via TLS"], correct: 1, explanation: "Bootstrap, mDNS, and rendezvous are alternative or complementary discovery mechanisms." },
        { question: "Why can a single peer have multiple multiaddrs?", options: ["It cannot; each peer has one multiaddr", "A peer can be reachable over different interfaces and transports (e.g. IPv4 and IPv6, or TCP and WebSocket)", "Only for redundancy in the DHT", "Only when using QUIC"], correct: 1, explanation: "One peer can advertise several addresses (different IPs, transports, or ports)." },
        { question: "What is the main benefit of protocol negotiation (e.g. multistream-select) when opening a stream?", options: ["It replaces the need for security", "Peers can agree on which application protocol and version to use, enabling versioning and backward compatibility", "It selects the transport", "It finds the closest DHT node"], correct: 1, explanation: "Negotiation lets both sides agree on a protocol (e.g. /myapp/1.0.0) so upgrades are possible without breaking old peers." },
        { question: "In Kademlia, how does lookup scale with network size n?", options: ["O(n)", "O(log n) hops to find the closest nodes to a key", "O(1) always", "O(sqrt(n))"], correct: 1, explanation: "Kademlia's structure (XOR distance, k-buckets) gives logarithmic lookup." },
        { question: "When is WebRTC useful as a libp2p transport?", options: ["Only for servers", "For browser-to-browser communication and NAT traversal (e.g. hole punching)", "Only for the DHT", "Only with TLS"], correct: 1, explanation: "WebRTC helps in browsers and behind NATs where direct TCP is difficult." },
        { question: "What role do bootstrap nodes play?", options: ["They run the DHT only", "Initial points of contact; peers connect to them first, then use the DHT and gossip to discover more peers", "They replace PeerId", "They are only for content routing"], correct: 1, explanation: "Bootstrap nodes are the initial entry points; from there the network is discovered." }
    ];
    initializeQuiz('quiz-libp2p-intermediate', questions, 70);
})();
